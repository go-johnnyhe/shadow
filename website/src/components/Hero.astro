---
import { dmgUrl } from '../consts';
import AppMockup from './AppMockup.astro';
---

<section class="hero">
  <div class="section-shell hero-inner">
    <div class="hero-copy">
      <span class="eyebrow-chip reveal">for developers who pair</span>

      <h1 class="hero-title reveal r-d1">
        Pair program without<br />
        <em>leaving your editor.</em>
      </h1>

      <p class="hero-desc reveal r-d2">
        Shadow syncs your project files to your teammate's machine in real time.
        One click to start, one link to share.
      </p>

      <div class="hero-actions reveal r-d2">
        <a
          href={dmgUrl}
          class="primary-button"
          data-cta
          data-cta-source="hero"
          data-cta-target="download_mac"
          data-cta-surface="website"
        >
          Download for Mac
        </a>

        <span class="hero-muted">
          Requires macOS 13+. Also available as a <a href="#install" class="muted-link">CLI</a>.
        </span>
      </div>
    </div>

    <div class="hero-mockup reveal r-d3">
      <AppMockup />
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(8rem, 18vh, 16rem);
    padding-bottom: clamp(5rem, 12vh, 10rem);
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 4rem;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .hero-mockup {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(2.8rem, 7vw, 5rem);
    font-weight: 700;
    line-height: 1.08;
    letter-spacing: -0.04em;
    color: var(--text);
  }

  .hero-desc {
    font-size: 1.25rem;
    line-height: 1.7;
    margin-top: 2rem;
    max-width: 38rem;
    color: var(--text-secondary);
  }

  .hero-actions {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    margin-top: 3rem;
  }

  .hero-muted {
    font-size: 0.82rem;
    color: var(--text-muted);
  }

  .muted-link {
    color: var(--text-secondary);
    text-decoration: underline;
    text-decoration-color: var(--border-strong);
    text-underline-offset: 2px;
    transition: color 0.2s ease;
  }

  .muted-link:hover {
    color: var(--text);
    opacity: 1;
  }

  @media (max-width: 900px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero-mockup {
      justify-content: flex-start;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 5.5rem;
      padding-bottom: 4rem;
    }

    .hero-title {
      font-size: clamp(2.2rem, 9vw, 2.8rem);
    }

    .hero-desc {
      font-size: 1.1rem;
    }
  }
</style>
